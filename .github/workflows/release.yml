name: Publish releases on new tag

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
    #tags:
    #  - '*'

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Install deps
        run: sudo apt-get install -y --no-install-recommends man-db pandoc rpmlint rpm apt-utils xmlstarlet lintian
